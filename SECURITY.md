# Security Policy

## 安全边界

- 不提交密钥、令牌、账号密码、Cookie、客户资料或个人隐私。
- 配置文件只保留安全默认值与占位示例。
- 涉及删除、移动、覆盖、压缩和重命名的脚本必须默认 Preview。
- Apply、Replace 和 Undo 必须使用显式模式；高风险覆盖/恢复操作还要使用确认令牌。
- 覆盖前必须保留备份；批量操作必须支持事务回滚或反向日志。
- 重复下载和疑似冗余素材只能隔离，不得在无人值守模式下永久删除。
- CI 必须验证失败注入后原文件、旧目标和目录结构仍可恢复。
- 自动更新地址必须指向本仓正式发布路径。

## 漏洞报告

请不要在公开 Issue 中粘贴真实凭据或敏感文件。优先使用仓库 Security 页面；无法使用时，通过 GitHub 私下联系维护者并提供最小复现、影响范围和建议修复方式。

如果秘密已被提交，请先轮换秘密，再处理代码与历史记录。
