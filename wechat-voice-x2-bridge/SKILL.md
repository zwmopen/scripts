---
name: mouse-voice-typing
description: 本地 Windows 常驻小软件及其运维技能。当前把鼠标 X2 侧键联动微信输入法语音输入，并规划升级为多组鼠标/键盘映射管理器。用于安装、启动、停止、开机自启、守护恢复、升级、排查或规划“鼠标快速语音打字”“X2 侧键语音输入”“鼠标键盘映射管理”等问题。
---

# 鼠标快速语音打字

## 定位

这是一个常驻后台的本地 Windows 小软件：把鼠标 `X2` 侧键变成微信输入法语音输入入口。配套技能负责帮助 Codex 安装、运行、诊断、修复和升级这个软件。

它解决的是微信输入法对普通模拟快捷键不稳定识别的问题。脚本监听 Windows 的 `XBUTTON2`，在松开鼠标侧键时通过 Windows `SendInput` 批量发送 `左 Ctrl + 左 Alt + O`，匹配微信输入法“启动语音输入”的快捷键。

## 产品边界与升级方向

- 把监听器、守护器、安装与恢复入口视为同一个小程序的内部模块，不拆成多个产品；对外提供统一的安装、启动、配置、诊断和卸载体验。
- 下一阶段升级为配置驱动的输入映射引擎，管理多组“鼠标/键盘触发 → 快捷键或动作”，而不是继续为每个映射复制一套脚本。
- 保留 `X2 → 左 Ctrl + 左 Alt + O → 微信输入法语音输入` 作为首个内置预设，并保证旧配置兼容。
- 后续优先支持启停单条映射、应用范围、冲突检测、统一日志和托盘管理；内部模块可继续分离以保持稳定性。

## 使用入口

脚本目录：

```text
D:\AICode\AI\skills\技能包\技能\AI协作\鼠标快速语音打字\scripts
```

常用入口：

- `scripts\start.cmd`：启动后台监听。
- `scripts\stop.cmd`：停止后台监听。
- `scripts\install-startup.cmd`：安装当前用户开机自启动。
- `scripts\uninstall-startup.cmd`：取消开机自启动。
- `scripts\watchdog.ps1`：守护脚本，发现后台监听不在时自动拉起。
- `scripts\install-watchdog.ps1`：安装计划任务守护；登录时启动，每 5 分钟检查一次，并在解锁/睡眠恢复后主动重启监听。
- `scripts\uninstall-watchdog.ps1`：取消计划任务守护和恢复刷新任务。
- `scripts\run-hidden.vbs`：隐藏运行 PowerShell，避免计划任务弹出蓝色窗口。
- `scripts\wechat-voice-x2-bridge.ps1`：核心脚本。
- `scripts\repair-and-status.ps1`：一键重启并执行心跳健康检查，显示成功或失败结果。
- `scripts\install-desktop-shortcut.ps1`：创建桌面“鼠标语音输入-启动修复”入口。

## 运行规则

1. 保持微信输入法语音输入快捷键为 `左 Ctrl + 左 Alt + O`。
2. 启动脚本后，按鼠标 `X2` 侧键即可唤起微信输入法语音输入。
3. 默认推荐安装计划任务守护，这样登录、重启、熄屏解锁、睡眠恢复后仍可直接使用。
4. 如果用户说“X2 没反应”，先检查脚本是否运行、PID 文件是否存在、日志是否记录 `XBUTTON2`。

## 本地运行文件

运行时会生成：

- `scripts\wechat-voice-x2-bridge.log`
- `scripts\wechat-voice-x2-bridge.pid`
- `scripts\wechat-voice-x2-bridge.heartbeat`

这两个文件只代表本机当前运行状态，不是技能源码，不需要提交或同步。

## 排查顺序

1. 确认微信输入法设置里“启动语音输入”为 `左 Ctrl + 左 Alt + O`。
2. 确认后台脚本正在运行。
3. 查看日志是否出现 `Mouse down XBUTTON2`、`Mouse up XBUTTON2`、`SendInput sent LeftCtrl+LeftAlt+O`。
4. 如果有侧键日志但没有弹窗，优先怀疑微信输入法快捷键设置变化或目标窗口权限层级差异。
5. 如果没有侧键日志，优先怀疑鼠标驱动、侧键映射或脚本未运行。

## 兼容性记录

- 2026-07-26 / v0.4.0：增加钩子与轮询双通道、15 秒心跳、60 秒钩子刷新；守护任务使用心跳识别“进程假活”，并改为直接 PowerShell 任务。增加桌面一键启动修复入口。
- 2026-07-19：修复“进程仍在但钩子失活”和守护竞态。底层鼠标回调只入队，日志与快捷键发送改由后台线程处理；监听器和守护程序增加单实例锁。
- 2026-07-18：快捷键注入由旧式 `keybd_event` 升级为 `SendInput`，提高新版微信输入法的识别稳定性，并在日志中记录发送数量或 Win32 错误码。

## 分类

- 所属大类：AI协作 / 人机协同
- 资产形态：本地常驻小软件
- 技能类型：软件运维型技能（内含 PowerShell/C# 脚本）
- 场景：输入效率、语音转文字、鼠标快捷操作、微信输入法联动

## 资产状态

- 资产名称：鼠标快速语音打字
- 触发链：鼠标 `X2`（Windows `XBUTTON2`）→ `左 Ctrl + 左 Alt + O` → 微信输入法语音输入
- 持久化方式：当前用户登录启动、每 5 分钟守护、解锁/睡眠恢复后刷新监听
- 云端镜像：`https://github.com/zwmopen/scripts/tree/codex/wechat-voice-x2-bridge`
- 归档状态：已整理为可复用的小技能，源码、技能说明和归档卡位于同一资产目录

详细资产卡见同目录的 `ASSET.md`。
